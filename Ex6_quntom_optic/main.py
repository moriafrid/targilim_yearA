# This is a sample Python script.

# Press Ctrl+F5 to execute it or replace it with your code.
# Press Double Shift to search everywhere for classes, files, tool windows, actions, and settings.


def print_hi(name):
    # Use a breakpoint in the code line below to debug your script.
    print(f'Hi, {name}')  # Press F9 to toggle the breakpoint.


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    print_hi('PyCharm')

    #for photon_nam in np.arange(maxepsilon):
    #    correlation=0
    #    index=np.where(ex_photon==photon_nam)
    #    index = np.array(index).T
        #g2.append((np.mean(ars.T*ats))/(np.mean(ars)*np.mean(ats)))
        #ar_p=ars[np.array(index)]
        #at_p=ats[index]

    #    for i in index:
    #        if ars[int(i)]==ats[int(i)] and ars[int(i)]>1:
    #            correlation+=1
    #    chanc2correlation=correlation/len(index)
    #    ydata.append(chanc2correlation)

# See PyCharm help at https://www.jetbrains.com/help/pycharm/
